jupyterhub:
  hub:
    services:
      binder:
        apiToken: "<apiToken>"
  proxy:
    secretToken: "<secretToken>"

registry:
  url: https://<acr-login-server>
  username: <username>
  password: <password>
